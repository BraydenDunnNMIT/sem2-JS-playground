/*
deleteBtn.addEventListener('click', e => {
let id = e.target.parentElement.getAttribute('data-id');
db.collection('todo').doc(id).delete();
})
*/
//Adding the delete function in a callback function inside an event listener. 
